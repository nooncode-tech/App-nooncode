# Browser validation 2026-05-16 — B1.3a outbound smoke E2E ($1 real)

**Iteration:** `fase-1-b1-stripe-live-cutover` sub-iteration B1.3a (outbound smoke).
**Spec:** `specs/fase-1-b1-stripe-live-cutover.md` §B1.3 (Día 4 smoke E2E real).
**Scope:** outbound flow only — seller crea checkout link, cliente paga out-of-band, App procesa webhook → activación → earnings credit. Inbound flow (NoonWeb-driven) deferido a B1.3b en iteración separada con coord NoonWeb dev.
**Step 7 deferred:** withdraw via Stripe Connect imposible hoy — ningún seller tiene `stripe_connect_account_id` onboarded (verificado pre-flight). Diferido a B1.3b iteración separada cuando Connect esté onboarded para al menos un seller.

**Environment:**
- Production deploy URL: `https://nooncode-app-pi.vercel.app`.
- Supabase project: `pdotsdahsrnnsoroxbfe` (live mode).
- Stripe Live mode keys configuradas en Vercel scope `Production` post-B1.1 rotation/scope-split.
- `STRIPE_WEBHOOK_SECRET` (whsec_*) subido a Vercel scope `Production only` 2026-05-16 (B1.2 paso 1 ejecutado por Pedro).
- Webhook endpoint Stripe Dashboard Live mode: `NoonApp production webhook` → `https://nooncode-app-pi.vercel.app/api/webhooks/stripe` (`we_1TXpLvRC5LvlmWeuVxdXmOoh`), 6 eventos suscritos (`checkout.session.completed`, `payment_intent.payment_failed`, `charge.refunded`, `account.updated`, `transfer.paid`, `transfer.reversed`).

---

## Pre-flight evidence

### Query A — Stripe Connect status

```
admin@noon.app    | NULL | none | active
maria@noon.app    | NULL | none | active
juan@noon.app     | NULL | none | active
```

Ningún seller tiene Connect → Step 7 deferred.

### Query B — pre-existing live session

```
payment_id: 96c3b6c8-9e86-4be5-9e6b-340dd4de2291
proposal_id: 9e86997d-7415-4273-9f35-b675acdad1bd
status: pending
amount: 35000.00 USD
session: cs_live_a1BfLygQ82EmKAuS5TQKErv8LPIsPTTQ2DwKtlNjAa18Wl4F0WbQ4XU0RH
expires: 2026-05-17 19:31:09 UTC
```

Landmine: este session de **$35,000 USD live** dangling de la validación F-V08 (mistake de interpretación del wire payload — `amount: 35000` se leyó como $350 cuando es $35,000). Expirado manualmente vía Stripe Dashboard Live por el operador (Pedro) ANTES de empezar B1.3a.

### Query C — webhook ledger baseline

```
total_rows: 0
live_rows: 0
processed_rows: 0
last_received_at: NULL
```

Empty baseline — cualquier row nueva post-smoke es atribuible a nuestra acción.

---

## Scenarios (mark each PASS/FAIL/PENDING as we go)

### Scenario 0 — Pre-smoke setup

**Steps:**
- [ ] cs_live $35K dangling session expirada via Stripe Dashboard.
- [ ] Pedro tiene su tarjeta lista (con OK para USD internacional, máximo $1 USD).
- [ ] Pedro logueado en `https://nooncode-app-pi.vercel.app` como `juan@noon.app` (seller).
- [ ] Tab paralelo: Stripe Dashboard Live mode → Webhooks → endpoint detail (para ver el counter de "Entregas de eventos").
- [ ] Tab paralelo: Supabase SQL Editor abierto.
- [ ] Tab paralelo: DevTools Network del browser.

**Verdict:** PENDING.

---

### Scenario 1 — Crear lead outbound

**Steps:**
1. En `/dashboard/leads` → click "Nuevo Lead".
2. Datos:
   - `Nombre`: `B1.3a Smoke Test`
   - `Email`: `qa.smoke@nooncode.com`
   - `Empresa`: `B1.3a Test Co`
   - `Origen`: outbound (debería ser default para sales)
   - `Score`: cualquier valor.
3. Guardar.

**Verification SQL:**
```sql
select id, name, email, company, lead_origin, assigned_to, created_at
from public.leads
where name = 'B1.3a Smoke Test'
order by created_at desc
limit 1;
```

Esperado: 1 fila, `lead_origin = 'outbound'`, `assigned_to = <juan-profile-id>`.

**Lead ID:** `2b46b7f8-9728-4502-bac4-d1455b20ff84`. `lead_origin=outbound`, `assigned_to=ff0ecbc0-7baa-4650-b93e-0bb952ee00e2` (juan), `name='Smoke Test'`, `email='qa.smoke@nooncode.com'`, `company=NULL`. Creado `2026-05-16 22:05:20 UTC`.

**Verdict:** PASS.

---

### Scenario 2 — Crear proposal sent

**Steps:**
1. Click sobre el lead nuevo → tab `Propuesta`.
2. Crear propuesta:
   - `Título`: `B1.3a Smoke proposal`
   - `Amount`: **`1`** (¡importante! 1, no 100 — porque la columna stores dollars; el route convierte a cents internamente).
   - `Body`: `B1.3a smoke test proposal — refund expected after webhook validation.`
   - `Currency`: USD (default).
   - `Status`: sent.
   - **`Seller fee`: 100** (default OK — el menor valor del state machine).
3. Save.

**Verification SQL:**
```sql
select
  lp.id,
  lp.title,
  lp.amount,
  lp.currency,
  lp.status,
  lp.review_status,
  lp.payment_status,
  sf.amount as seller_fee_amount,
  sf.state as seller_fee_state
from public.lead_proposals lp
left join public.seller_fees sf on sf.proposal_id = lp.id
where lp.title = 'B1.3a Smoke proposal'
order by lp.created_at desc
limit 1;
```

Esperado: 1 fila, `status='sent'`, `review_status='pending_review'`, `payment_status=null`, `amount=1`, `seller_fee_amount=100`, `seller_fee_state='potential'`.

**Proposal ID:** `3860ace8-46a8-4345-88cd-bb134b6aeee3`. `amount=1.00`, `currency=USD`, `status=sent`, `review_status=pending_review`, `payment_status=NULL`, `seller_fee_amount=100.00`, `seller_fee_state=potential`. La seller_fees row se creó automáticamente al crear el proposal (B3 state machine integration funciona).

**Verdict:** PASS.

---

### Scenario 3 — PM approve

**Steps:**
1. Logout / browser incógnito → login como `admin@noon.app` (admin actúa como PM porque ningún user es role pm en prod).
2. `/dashboard/pm-queue` o ir directo al lead → propuesta.
3. Aprobar la propuesta del Scenario 2.

**Note:** el PM queue path de aprobación es para inbound. Para outbound, la review/approve happens via la API route `/api/proposals/[proposalId]/review` con `action: 'approve'`. Lo más probable es que la UI permita aprobación directa desde lead detail tab `Propuesta` (con un botón "Aprobar"). Si no aparece, fallback es hacer el PATCH manualmente desde DevTools console o usar la SQL UPDATE como workaround.

**Workaround SQL (si la UI no permite):**
```sql
update public.lead_proposals
set review_status = 'approved',
    reviewer_id = '<admin-profile-id>',
    reviewed_at = now()
where id = '<proposal-id>';
```

**Verification SQL:**
```sql
select id, review_status, reviewer_id, reviewed_at
from public.lead_proposals
where id = '<proposal-id>';
```

Esperado: `review_status='approved'`, `reviewer_id` poblado, `reviewed_at` poblado.

**Resultado:** `review_status=approved`, `reviewer_id=dae88316-6eea-4997-bc50-a71cbec25c3d` (admin), `reviewed_at=2026-05-16 22:13:41 UTC`. `status` sigue `sent`. `payment_status` sigue `NULL`.

**Verdict:** PASS.

---

### Scenario 4 — Crear checkout link

**Steps:**
1. Volver a Juan (logout admin / browser incognito → login juan@noon.app).
2. Lead nuevo → tab `Propuesta` → propuesta aprobada.
3. Click "**Crear link de pago**".
4. UI debe transicionar a Estado 2 (active) per F-V08 — `Copiar link` + `Abrir link` + chip `Vence el …`.
5. **NO ABRIR EL LINK TODAVÍA**. Solo copiar la URL.

**Verification SQL:**
```sql
select
  p.id,
  p.proposal_id,
  p.amount,
  p.status,
  p.stripe_checkout_session_id,
  p.stripe_checkout_url,
  p.stripe_checkout_expires_at,
  p.stripe_customer_id
from public.payments p
where p.proposal_id = '<proposal-id>'
order by p.created_at desc
limit 1;
```

Esperado:
- 1 fila nueva con `status='pending'`.
- `amount = 1.00`.
- `stripe_checkout_session_id` empieza con `cs_live_`.
- `stripe_checkout_url` URL completa de Stripe Checkout.
- `stripe_checkout_expires_at` ~24h en el futuro.
- `stripe_customer_id` poblado (cus_xxxx).

**Checkout session ID:** `cs_live_a1TJpTZDzoCXn2YicriU7Q7qCffX7cvi1cYengT5iG5LulI8Lv1WHFpuaL`. Payment row id: `f15232ba-d3d8-4310-87df-0d816b9cae54`. Customer: `cus_UWucYadZYxbV4w`. Amount $1.00, status pending.

**Critical finding**: `stripe_checkout_url` quedó NULL y `stripe_checkout_expires_at` quedó NULL — Vercel Production está corriendo código pre-F-V08 (G11 landmine confirmado: ningún auto-deploy disparó en los merges de F-V08/V07/G8 de hoy). El redeploy de B1.2 esta mañana usó el deploy anterior. La UI igual mostró el toast "Link copiado" porque ese comportamiento es pre-F-V08. La URL se obtuvo del clipboard / Stripe Dashboard para continuar el smoke.

**Action item post-smoke**: redeploy production con develop head (`599d223`) para activar F-V08 + F-V07 + G8.

**Verdict:** PASS parcial (session creada en Stripe Live, todas las columnas pre-F-V08 OK; las 2 columnas F-V08 nuevas quedaron NULL porque deploy stale).

---

### Scenario 5 — Pagar el link con tarjeta real

**Steps:**
1. Abrir el URL del Scenario 4 en ventana incógnito.
2. Pagar $1 con tarjeta real.

**Verdict:** **DEFERRED 2026-05-16**.

**Razón**: el operador (Pedro) no tenía tarjeta disponible para hacer el cobro en el momento del smoke. Pre-flight Scenarios 1-4 confirmaron que la integración con Stripe Live mode está operativa (cs_live_* generado, cus_* customer creado, payment row en DB con status pending, B3 state machine seller_fee row en state `potential`). Lo único que queda sin validar son las Scenarios 5-9 que dependen de un pago real disparando un webhook real.

**Camino para cerrar el smoke** (cuando un operador con tarjeta esté disponible):
1. Loguearse como `juan@noon.app` en prod.
2. Abrir el lead `Smoke Test` (ID `2b46b7f8-9728-4502-bac4-d1455b20ff84`) → tab Propuesta.
3. La session vieja `cs_live_a1TJp...` ya está expirada (operador la expiró en cleanup) → la UI debe estar en Estado 3 (expired) o Estado 1 (none, si el `payments` row se marcó failed).
4. Click "Crear link de pago" / "Crear link nuevo" → genera nueva session.
5. Pagar con tarjeta real.
6. Verificar Scenarios 6-9 desde acá. La estructura del doc se preserva — solo cambia el `cs_live_*` ID.

---

### Scenario 6 — Webhook arrives + ledger processed

**Verdict:** **DEFERRED 2026-05-16** (depends on Scenario 5).

**Steps:**
1. Inmediatamente después del Scenario 5, ir al Stripe Dashboard Live → Webhooks → NoonApp production webhook → ver "Entregas de eventos" counter.
2. Refresh la página un par de veces durante los siguientes 30 segundos.

**Esperado:** counter pasa de `Total 0` a `Total 1` (o más, si Stripe disparó múltiples eventos como `payment_intent.created` + `charge.succeeded` + `checkout.session.completed`). Tu endpoint solo recibe los que estén suscritos:
- `checkout.session.completed` — debería llegar.

3. SQL Editor query del ledger:

```sql
select
  event_id,
  event_type,
  livemode,
  status,
  attempt_count,
  received_at,
  processed_at,
  last_error
from public.stripe_webhook_events
order by received_at desc
limit 5;
```

Esperado:
- Al menos 1 fila nueva.
- `event_type = 'checkout.session.completed'`.
- `livemode = true`.
- `status = 'processed'`.
- `attempt_count = 1`.
- `processed_at` poblado.
- `last_error = null`.

**Verdict:** PENDING.

**Si llega con `status = 'failed'`** → diagnosticar `last_error`. Casos comunes:
- "Signature mismatch" → `STRIPE_WEBHOOK_SECRET` mal copiado en Vercel.
- "Body parse failed" → bug en signature verification.
- "RPC failed" → bug en activación. Reconciliar manualmente con SQL.

---

### Scenario 7 — Activación end-to-end

**Verdict:** **DEFERRED 2026-05-16** (depends on Scenario 5+6).

**Verification SQL** (correr ~10 segundos después del Scenario 6):

```sql
-- a) payment row transitionada
select id, proposal_id, status, payment_intent_id, charge_id
from public.payments
where proposal_id = '<proposal-id>'
order by created_at desc
limit 1;
-- Esperado: status='succeeded', payment_intent_id poblado, charge_id poblado.

-- b) proposal row updated
select id, status, payment_status, paid_at, handoff_ready_at
from public.lead_proposals
where id = '<proposal-id>';
-- Esperado: payment_status='succeeded', paid_at poblado.

-- c) seller_fee transitionada
select id, proposal_id, state, amount, payment_id, confirmed_at
from public.seller_fees
where proposal_id = '<proposal-id>';
-- Esperado: state='confirmed', payment_id poblado, confirmed_at poblado.

-- d) wallet ledger entries
select id, account_id, amount_cents, entry_type, reference_type, reference_id, occurred_at
from public.wallet_ledger_entries
where reference_id::text = '<proposal-id>' OR reference_id::text = '<payment-id>' OR reference_id::text = '<seller-fee-id>'
order by occurred_at desc;
-- Esperado: entries con earnings_distribution para el seller juan.

-- e) project created (si la proposal era handoff_ready o si la activation lo crea automatic)
select id, name, status, source_proposal_id, source_lead_id, created_at
from public.projects
where source_proposal_id = '<proposal-id>';
-- Esperado: 1 row si activación crea proyecto; 0 rows si no.
```

**Verdict:** PENDING per check.

---

### Scenario 8 — UI verification

**Verdict:** **DEFERRED 2026-05-16** (depends on Scenario 5+6+7).

**Steps:**
1. Logueado como `juan@noon.app` → `/dashboard/leads` → lead "B1.3a Smoke Test".
2. Tab `Propuesta` → debería mostrar badge verde **"Pago confirmado"** (Estado 4 paid del F-V08 state machine).
3. `/dashboard/earnings` como juan → debería ver entry del earnings credit (pending o available_to_withdraw según FASE 3 lifecycle — actualmente pending hasta consolidación manual del admin).

**Verdict:** PENDING.

---

### Scenario 9 — Refund (cleanup)

**Verdict:** **DEFERRED 2026-05-16** (no payment to refund — Scenario 5 deferred).

**Steps:**
1. Stripe Dashboard Live mode → Payments → buscar el charge del Scenario 5 → click → **Refund** → Full amount → submit.
2. Esperar ~10s.
3. Refrescar Stripe Dashboard → Webhooks → endpoint → counter sube otra vez (esta vez por `charge.refunded`).

**Verification SQL:**
```sql
-- Ledger refund event
select event_id, event_type, status, last_error
from public.stripe_webhook_events
where event_type = 'charge.refunded'
order by received_at desc
limit 1;
-- Esperado: status='processed'.

-- payments row reverted? Depends on impl.
select id, status from public.payments where id = '<payment-id>';
-- Si la app tiene refund handler: status='refunded'. Si no: queda en 'succeeded'.
```

**Verdict:** PENDING.

---

## Summary table

| # | Scenario | Verdict |
|---|---|---|
| 0 | Pre-smoke setup | PASS |
| 1 | Crear lead outbound | PASS |
| 2 | Crear proposal sent | PASS |
| 3 | PM approve | PASS |
| 4 | Crear checkout link | PASS (parcial — `stripe_checkout_url` y `stripe_checkout_expires_at` NULL porque deploy stale pre-F-V08) |
| 5 | Pagar el link con tarjeta real | DEFERRED — operador sin tarjeta |
| 6 | Webhook arrives + ledger processed | DEFERRED (depende de #5) |
| 7 | Activación end-to-end | DEFERRED (depende de #5+#6) |
| 8 | UI verification | DEFERRED (depende de #5+#6+#7) |
| 9 | Refund (cleanup) | DEFERRED (no payment to refund) |

**Result:** **PARTIAL** — Steps 1-4 PASS (cs_live session creada en Stripe Live, customer creado, payment row pending, seller_fee row state=potential). Steps 5-9 DEFERRED awaiting card. La validación crítica de B1.2 (webhook signature verification end-to-end con un evento real) sigue pendiente.

## Post-smoke action items

1. **Operador**: expirar la session live `cs_live_a1TJpTZDzoCXn2YicriU7Q7qCffX7cvi1cYengT5iG5LulI8Lv1WHFpuaL` via Stripe Dashboard Live → Payments → Checkout Sessions → Expire, o via Workbench Shell `stripe checkout sessions expire <id>`. Evita que quede pagable accidentalmente.
2. **Operador**: redeploy producción con `develop` head (commit `599d223` o el más reciente) para activar F-V08 + F-V07 + G8 en prod. El deploy actual de Vercel está pre-2026-05-16 — confirmado por `stripe_checkout_url=NULL` post-creación de session (F-V08 escribe esa columna). El redeploy debería desbloquear que el state machine de 4 estados aparezca en prod cuando se retome el smoke.
3. **Próxima sesión cuando hay tarjeta**: retomar el smoke desde Scenario 5 — el lead `2b46b7f8-9728-4502-bac4-d1455b20ff84` y la proposal `3860ace8-46a8-4345-88cd-bb134b6aeee3` quedan en DB pre-pago, sin pollution. Click "Crear link de pago" / "Crear link nuevo" en la UI genera nueva session live → pagar $1 → resto del flow.
4. **DB cleanup deferred**: lead/proposal/payment rows quedan en DB sin tocar (opción A del cleanup tree — operador eligió retomar el smoke con el mismo lead cuando vuelva). Si en algún momento se prefiere borrar, ver §Reconciliation queries abajo.

---

## Reconciliation queries (use only if scenarios fail and we need to recover state)

### If webhook fails to activate (`stripe_webhook_events.status='failed'`)

Manually force activation:

```sql
-- Mark payment succeeded
update public.payments
set status = 'succeeded'
where stripe_checkout_session_id = '<cs_live_id>';

-- Mark proposal paid
update public.lead_proposals
set payment_status = 'succeeded', paid_at = now()
where id = '<proposal-id>';

-- Confirm seller_fee
update public.seller_fees
set state = 'confirmed', payment_id = '<payment-id>', confirmed_at = now()
where proposal_id = '<proposal-id>';
```

After reconciliation, fix the root cause (likely signature mismatch → re-verify `STRIPE_WEBHOOK_SECRET` in Vercel matches `whsec_*` from Stripe Dashboard endpoint config), redeploy, retry the webhook from Stripe Dashboard.

### If full rollback needed (everything failed, want to back out)

```sql
-- Refund via Stripe Dashboard first, then:
delete from public.seller_fees where proposal_id = '<proposal-id>';
delete from public.wallet_ledger_entries where reference_id::text = '<proposal-id>';
update public.lead_proposals set payment_status = null, paid_at = null where id = '<proposal-id>';
update public.payments set status = 'failed' where proposal_id = '<proposal-id>';
delete from public.projects where source_proposal_id = '<proposal-id>';
-- Optional: hard delete lead + proposal for cleanup
delete from public.lead_proposals where id = '<proposal-id>';
delete from public.leads where id = '<lead-id>';
```

---

## Notes

- This is the FIRST live transaction processed by NoonApp. Until now, the `stripe_webhook_events` ledger has been empty (Query C baseline). Whatever happens here is the source-of-truth evidence for B1.2/B1.3 closure.
- The session that gets created in Scenario 4 will be `cs_live_*`. This is a real Stripe Live session capable of charging a real card. The $1 amount limits blast radius.
- Step 7 (Connect withdraw) is impossible today because no seller has `stripe_connect_account_id` populated. Logged as B1.3b for a follow-up iteration when Stripe Connect onboarding is set up for at least one seller.
- This validates B1.2 implicitly: if Scenario 6 PASSes (`livemode=true, status=processed`), the webhook signature verification works end-to-end, which is the substance of B1.2. Adding an explicit B1.2 verification later is no longer required.
